# MDN
- the URLSearchParams defines the utility methods to work withthe query string of a URL
- toString() is a method or URLSearchParams that returns a query string for use of a URL
# using url hash
- lacation interface represents the location of the object it is linked to
- locations.reload is basically a refresh button
- hashshage is run when the fragment identifier of the url has changed.